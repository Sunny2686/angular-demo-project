import { PractiseDirective } from './practise.directive';

describe('PractiseDirective', () => {
  it('should create an instance', () => {
    const directive = new PractiseDirective();
    expect(directive).toBeTruthy();
  });
});
